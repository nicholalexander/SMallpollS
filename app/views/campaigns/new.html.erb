Lets create a new campaign!

<%= form_for [:organization, @campaign] do |f| %>
    <%= f.label "Name" %>
    <%= f.text_field :name %>

    <%= f.label "Description" %>
    <%= f.text_field :description %>
    
    <%= f.submit "Create" %>
<% end %>